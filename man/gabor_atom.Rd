% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gabor_atom.R
\name{gabor_atom}
\alias{gabor_atom}
\title{Generate a Gabor atom}
\usage{
gabor_atom(
  number_of_samples,
  sampling_frequency,
  mean,
  phase,
  sigma,
  frequency,
  normalization = TRUE
)
}
\arguments{
\item{number_of_samples}{Positive integer specifying the number of samples
in the generated Gabor atom.}

\item{sampling_frequency}{Sampling frequency in Hz.}

\item{mean}{Time position of the center of the Gaussian envelope, in seconds.}

\item{phase}{Phase of the sinusoidal component, in radians.}

\item{sigma}{Positive scale parameter controlling the width of the Gaussian
envelope, in seconds.}

\item{frequency}{Frequency of the sinusoidal component in Hz.}

\item{normalization}{Logical; if \code{TRUE}, the resulting Gabor atom is
normalized to unit Euclidean norm.}
}
\value{
A list containing four numeric vectors of length \code{number_of_samples}:

  \item{cosine}{Cosine wave.}
  \item{gauss}{Gaussian envelope.}
  \item{gabor}{Gabor function.}
  \item{time}{Time vector corresponding to the signal samples.}
}
\description{
Generates a real-valued Gabor atom consisting of a sinusoidal component
localized by a Gaussian envelope. Gabor atoms provide simultaneous
localization in time and frequency and are commonly used in
time-frequency dictionaries for Matching Pursuit decomposition.
}
\examples{
number_of_samples <- 512
sampling_frequency <- 256.0
mean <- 1
phase <- pi
sigma <- 0.5
frequency <- 5.0
normalization = TRUE

out <- gabor_atom(
  number_of_samples,
  sampling_frequency,
  mean,
  phase,
  sigma,
  frequency,
  normalization
)

# Verify unit-norm normalization
sqrt(sum(out$gabor^2))

plot(out$time, out$gabor, type = "l", xlab = "t", ylab = "gabor", panel.first = grid())

}
